# --- web / api ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.20
itsdangerous>=2.2,<3.0

# --- telegram ---
# aiogram — интерфейс управления (бот от @BotFather)
aiogram>=3.15,<4.0
# telethon — торговый контур MTProto.
# Версия >=1.41 обязательна: resale-API подарков
# (GetResaleStarGifts / UpdateStarGiftPrice / InputInvoiceStarGiftResale)
# появился только в поздних слоях.
telethon>=1.45,<2.0

# --- storage ---
SQLAlchemy>=2.0.30,<2.1
psycopg[binary]>=3.2,<4.0

# --- infra ---
httpx>=0.27,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0,<2.0
cryptography>=43.0
APScheduler>=3.11,<4.0

# --- tests ---
pytest>=8.3,<9.0
pytest-asyncio>=0.25,<1.0
